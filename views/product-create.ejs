<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Add Section</title>
  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      overflow: hidden;
    }

    .content {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background: #f8f9fa;
      position: relative;
    }

    .pattern {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('https://www.heropatterns.com/api?id=circuit-board') repeat;
      opacity: 0.1;
      pointer-events: none;
      animation: patternAnimation 20s linear infinite;
    }

    @keyframes patternAnimation {
      from {
        transform: rotate(0deg);
      }
      to {
        transform: rotate(360deg);
      }
    }

    form {
      background-color: rgba(255, 255, 255, 0.9);
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
      max-width: 300px;
    }

    .field {
      margin-bottom: 20px;
    }

    input {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    input[type="submit"] {
      background-color: #3498db;
      color: #fff;
      cursor: pointer;
    }

    input[type="submit"]:hover {
      background-color: #2980b9;
    }

    @media (min-width: 768px) {
      .content {
        max-width: 600px;
        margin: 0 auto;
      }
    }
  </style>
</head>
<body>
  <%- include("nav")%>
  <div class="content">
    <div class="pattern"></div>
    <form action="/product/create" method="post">
      <div class="field">
        <input type="text" name="title" placeholder="Title" required />
      </div>
      <div class="field">
        <input type="text" name="des" placeholder="Description" required />
      </div>
      <div class="field">
        <input type="url" name="img" placeholder="Image URL" required />
      </div>
      <div class="field">
        <input type="number" name="price" placeholder="Price" required />
      </div>
      <div class="field">
        <input type="submit" value="Add Product" />
      </div>
    </form>
  </div>
</body>
</html>
